let verbs = ['goes', 'runs']
let subjects = ['Reice', 'Jace']
let preps = ['to the store', 'to the bar']

const basicSentence = () => {
    return `${subjects[1]} ${verbs[1]} ${preps[1]}`
};

console.log(basicSentence())